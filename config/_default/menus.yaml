# Navigation — use pageRef for internal pages so links respect baseURL (e.g. GitHub Pages subpaths).
# See: https://gohugo.io/configuration/menus/

menus:
  main:
    - name: Home
      pageRef: /
      weight: 10
    - name: Blog
      pageRef: /blog
      weight: 20
    - name: Work
      pageRef: /uses
      weight: 30
